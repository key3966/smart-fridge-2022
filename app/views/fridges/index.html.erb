<%= render "shared/header"%>
<%= image_tag('testbanner.jpeg', class: 'img-fluid') %>

<% if user_signed_in? && current_user.fridge_id.nil? %>
  <h5>冷蔵庫を新規作成してください</h5>
  <%= form_with model: @fridge, local: true do |f| %>
  <%= render 'shared/error_messages', model: f.object %>
  <%= f.text_field :title, placeholder: "冷蔵庫の名前を入力してください", class: 'form-control' %>
  <div class="text-end">
    <%= f.submit "新規作成", class: 'btn btn-primary' %>
  </div>
  <% end %>
<% else %>
<% end %>

<%# 冷蔵庫アイテム一覧 %>
<% if user_signed_in? && current_user.fridge_id.present? %>
  <% if current_user.fridge.title.blank? %>
    <div>
      <h3 class='your-fridge'>
        <%= current_user.nickname %>さんの冷蔵庫
      </h3>
    </div>
  <% else %>
    <div>
      <h3 class='your-fridge'>
        <%= current_user.fridge.title %>
      </h3>
    </div>
  <% end %>
<% end %>
<%# link_to "買い物へ行く", #path(@fridge.id) %>

<div>
  <h6>新規アイテム登録</h6>
</div>

<%= form_with local: true do |form| %>
  <table class="table table-striped table-hover table-sm">
    <thead>
      <tr>
        <th scope="col">レギュラー</th>
        <th scope="col">名前</th>
        <th scope="col">カテゴリー</th>
        <th scope="col">消費/賞味期限</th>
        <th scope="col">残量</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= form.check_box :hoge, {checked: true, class: "form-check-input", type: "checkbox", id: "checkboxNoLabel" }, "1", "0" %></td>
        <td><%= form.text_field :hoge, placeholder: "test", class: 'form-control' %></td>
        <td><%= form.text_field :hoge, placeholder: "test", class: 'form-control' %></td>
        <td><%= form.text_field :hoge, placeholder: "test", class: 'form-control' %></td>
        <td><%= form.text_field :hoge, class: 'form-control'%></td>
        <td><%= form.submit "登録" %></td>
    </tbody>
  </table>
  
<% end %>

<table class="table table-striped table-hover table-sm">
  <thead>
    <tr>
      <th scope="col">レギュラー</th>
      <th scope="col">名前</th>
      <th scope="col">カテゴリー</th>
      <th scope="col">消費/賞味期限</th>
      <th scope="col">残量</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <%= form_with local: true do |form| %>
      <tr>
        <td><%= form.check_box :hoge, {checked: true, class: "form-check-input", type: "checkbox", id: "checkboxNoLabel" }, "true", "false" %></td>
        <td><%= form.text_field :hoge, placeholder: "test", class: 'form-control' %></td>
        <td><%= form.text_field :hoge, placeholder: "test", class: 'form-control' %></td>
        <td><%= form.text_field :hoge, placeholder: "test", class: 'form-control' %></td>
        <td><%= form.text_field :hoge, class: 'form-control'%></td>
        <td><%= form.submit "更新" %></td>
      </tr>
      <tr>
        <td><%= form.check_box :hoge, {checked: true, class: "form-check-input", type: "checkbox", id: "checkboxNoLabel" }, "true", "false" %></td>
        <td>卵</td>
        <td>生鮮食品</td>
        <td>2/15</td>
        <td>少ない</td>
        <td><%= form.submit "更新" %></td>
      </tr>
      <tr>
        <th scope="row">ビール</th>
        <td>飲料</td>
        <td>5/15</td>
        <td>たくさん</td>
      </tr>
    <% end %>
  </tbody>
</table>

<%# /冷蔵庫アイテム一覧 %>